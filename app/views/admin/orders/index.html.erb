<h1>注文履歴</h1>

<table>
	<tr>
		<th>注文日時</th>
		<th>購入者</th>
		<th>注文個数</th>
		<th>注文ステータス</th>
	</tr>
	<tr>
		<% @orders.each do |i|%>
			<td><%= i.created_at %></td>
			<td><%= i.name %></td>
			<td><%= i.price %></td>
			<td><%= i.genre.name %></td>
			<td><%= i.is_active %></td>
	</tr>
		<% end %>
</table>